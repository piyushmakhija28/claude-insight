<!-- Generated by Phase 0.1 ast-graph-engineer -- do not edit manually -->
# AST Call Graph Summary -- claude-workflow-engine (2026-08-01)

## Scope
`langgraph_engine/`, `hooks/`, `scripts/`, `src/` per Phase 0.0's rule, excluding
`.venv/` and the 10 confirmed-dead `helper_nodes/` functions. 335/335 files parsed,
0 syntax errors.

## Node / edge counts
- Total nodes: **2,218** (1,110 functions + 961 methods + 96 nested fns + 51
  synthetic `__main__`/entry nodes)
- CHA edges (sound over-approximation): **9,351**
- RTA edges (import-closure restricted): **2,313** -- 75.3% reduction from CHA
- Andersen-lite edges (single-candidate exact resolution): **1,686** -- 27.1% further
  reduction, 82.0% cumulative reduction from CHA
- Unresolved call sites (stdlib/3rd-party/reflection, no live-scope candidate): 11,857

Python has no JVM declared-receiver-type, so CHA = "all live-scope fns/methods
sharing a bare name are candidates"; `self.method()` uses true MRO-based CHA (base
class + live subclass overrides). RTA/Andersen restrict by import visibility and
single-candidate resolution -- full statement in `refinement_stats.note`.

## Dynamic dispatch (LangGraph)
**27/27 `graph.add_node(id, fn)` registrations resolved** (0 unresolved) -> 27
dynamic-dispatch edges, all now entry points. Required fixing a relative-import bug
in this analyzer: a level-1 relative import inside a package's `__init__.py` (e.g.
`from .nodes import x` in `preflight_guard/__init__.py`) stays inside that package --
it does not go up a level, unlike from a plain module file. That fix alone raised
CHA-reachable nodes from 700 to 960.

## Reachability
- Entry points seeded: 122 (`main()` fns, `__main__` blocks, MCP `@tool` handlers,
  all 27 LangGraph registrations, synthetic entries for the 3 hook scripts +
  `3-level-flow.py` + `health_server.py`)
- Reachable under CHA (sound, proven-safe per skill's M2 theorem): **960/2,218 (43.3%)**
- `live_function_ids`: **909** (reachable minus synthetic entries)
- Per skill Anti-Pattern #1, liveness uses **CHA** not RTA/Andersen: reflection/
  dynamic patterns (LangGraph dispatch, hook subprocess entry, MCP decorators) exist
  outside full static modeling, so CHA is the only zero-false-positive choice.

## Audit surface
- Credential access sites: 62 (GITHUB_TOKEN 24, ANTHROPIC_API_KEY 26, FIGMA_ACCESS_TOKEN 12)
- `settings.json` touch sites: 17
- Subprocess spawn sites: 112 total, 17 inside `hooks/stop_notifier/` +
  `stop-notifier.py` -- static count consistent with (not proof of) the "Stop hook
  spawns 4+ subprocesses/turn" claim.

## Builder divergence (full detail: builder_divergence.md)
`call_graph_builder.py` was actually invoked: hard-caps at `MAX_FILES = 300`
(`parsers/config.py:11`). Traversal order means **`langgraph_engine/sdlc_pipeline/`
(45 files, the Level-2 SDLC execution core) is 100% absent** from its output, plus
38/45 hook-package files, while 75 of its 300-file budget goes to `tests/`. Verdict:
not reliable for sdlc_pipeline/hook-package impact analysis until the cap is fixed.

## Coverage limitation, stated explicitly
Resolution uses lightweight name/import matching, not a type checker; Andersen's
stage is points-to-lite (self.method + import bindings + decorator-wrapper unwrap).

## Top 5 uncertainty notes
1. 43.3% CHA-reachability is below the skill's typical 55-85% range; likely
   under-counts liveness since non-`self` attribute calls mostly fall to
   CHA-only/unresolved rather than points-to-tracked.
2. `scripts/tools/*.py` CLI utilities have no entry seeding beyond `main()`/
   `__main__` -- manually-invoked scripts may under-report as dead.
3. `getattr()`/`importlib` dynamic dispatch outside orchestrator.py/subgraph.py was
   not exhaustively re-verified.
4. Cyclomatic complexity is approximate (nested-function decisions may leak into the
   enclosing function's count via flat `ast.walk`).
5. `dead_function_ids_non_helper` (1,248) is NOT a REMOVE list -- CHA-unreached only,
   pending reflection/DI-liveness cross-check before any disposition tier.
