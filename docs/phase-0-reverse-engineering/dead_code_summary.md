<!-- Generated by Phase 0.0 codebase-archaeology-agent -- do not edit manually -->
# Dead Code Detection Summary -- claude-workflow-engine (2026-08-01)

## Counts per verdict (12 module-level items classified)
- REMOVE: 1 -- `langgraph_engine/helper_nodes/` (package, 4 files, 10 functions)
- ARCHIVE: 0
- REVIEW: 3 -- `langgraph_engine/call_graph_analyzer.py`, `langgraph_engine/quality_gate.py` (deprecated top-level shims, no live internal importer of the old path), `hooks/project_session.py` (liveness path asserted by pattern match, not fully traced)
- RETAIN: 9 -- `flow_state.py`, `call_graph_builder.py`, `uml_generators.py` (confirmed heavily imported), all 3 hook packages (`pre_tool_enforcer/`, `post_tool_tracker/`, `stop_notifier/`), `ide_paths.py`, `session_context.py`

Total files scanned: 411 Python files repo-wide (excl. `.venv/`): langgraph_engine 237, tests 75, hooks 45, scripts 37, src 16, root 1.

## hook_only_reachable (highest-value finding)
Three entire packages have **no Python import path** from `scripts/3-level-flow.py` or `langgraph_engine/` -- they are reachable **exclusively** through Claude Code's PreToolUse/PostToolUse/Stop hook mechanism (external, registered in `~/.claude/settings.json`):
1. `hooks/pre_tool_enforcer/` -- via `hooks/pre-tool-enforcer.py`
2. `hooks/post_tool_tracker/` -- via `hooks/post-tool-tracker.py`
3. `hooks/stop_notifier/` -- via `hooks/stop-notifier.py`

The only cross-references found outside `hooks/` (`langgraph_engine/context/flow_trace_converter.py`, `scripts/helpers/flow_trace_reader.py`) are comment-text mentions, not imports -- verified by reading the matched lines. **If PreToolUse/PostToolUse/Stop are ever unregistered or the three shim files deleted, these three packages go instantly dead with nothing else keeping them alive.** This is a primary input for the policy audit.

Note: `hooks/ide_paths.py`, `hooks/project_session.py`, and `hooks/session_context.py` live in the same directory but are **not** hook-only -- they have independent importers in `scripts/github_operations/`, `scripts/github_pr_workflow/`, and `langgraph_engine/context_sync/`.

## `helper_nodes/` verdict: REMOVE (independently verified, not inherited from CLAUDE.md)
Repo-wide grep for the literal string `helper_nodes` outside the package itself returns **zero matches**. Every one of its 10 functions has an identically-named, independently-implemented duplicate in `langgraph_engine/orchestrator.py` that is the one actually registered via `graph.add_node(...)` (e.g. `output_node` at orchestrator.py:363/779, `step5_retry_increment_node` at orchestrator.py:133/810). No `getattr()`-based dynamic dispatch found in `orchestrator.py` or `sdlc_pipeline/subgraph.py`. Confidence: HIGH.

## live_function_ids
Full function-level (3,985-method) enumeration was **not** performed -- out of scope for a grep/Read-only agent at this context budget. Operational filter handed to `ast-graph-engineer`: include all of `langgraph_engine/`, `hooks/`, `scripts/`, `src/` **except** `langgraph_engine/helper_nodes/*` (10 dead_function_ids listed explicitly in the JSON).

## Top 5 uncertainty notes
1. No full AST call-graph BFS was run; module/file-granularity grep reachability only.
2. `hooks/project_session.py`'s exact reachability from `scripts/3-level-flow.py` was pattern-matched, not import-chain-traced.
3. 15 of 16 files in `src/mcp/` were not individually import-traced (only `session_hooks.py` confirmed per CLAUDE.md).
4. `scripts/tools/*.py` one-off CLI utilities were not enumerated -- "reachability via import" is the wrong test for manually-invoked scripts.
5. The two deprecated top-level shims (`call_graph_analyzer.py`, `quality_gate.py`) could still have external (out-of-repo) callers using the old dotted path; kept at REVIEW rather than REMOVE for that reason.
