<!-- Generated by Phase C.1 (codebase-archaeology-agent), read-only static scan. -->
# Structural Inventory Summary (Phase C.1)

Scope: `langgraph_engine/`, `hooks/`, `scripts/`, `src/`, `.venv/` excluded. 335 live-scope `.py`
files (411 repo-wide, matches orchestrator's verified fact #1; CLAUDE.md's "395" is stale).

## Counts
- **Packages** (granularity: top-level dir + 1 subdir level): 39 (23 under `langgraph_engine/` incl.
  root, 4 under `hooks/`, 8 under `scripts/`, 3 under `src/`)
- **Modules (files)**: 335 — `langgraph_engine` 237, `hooks` 45, `scripts` 37, `src` 16
- **Classes**: 174 (`langgraph_engine` 151, `hooks` 2, `scripts` 7, `src` 14)
- **Public top-level functions**: 619; **Public methods**: 721 (total callable public surface: 1,340)
- `langgraph_engine/helper_nodes/` (5 files, 9 top-level functions, 0 classes) excluded from the
  "live" totals above per the task's dead-code carve-out — all 9 functions are shadowed by same-name
  copies in `orchestrator.py`, consistent with verified fact #4.

## Import cycles (module-import graph, NOT the call graph — Tarjan's SCC)
- **1 file-level cycle**, 9 members: `orchestrator.py` + `sdlc_pipeline/subgraph.py` +
  `sdlc_pipeline/nodes/__init__.py` + 3 node wrapper files + `sonar_auto_fixer.py` +
  `langgraph_engine/__init__.py` — the graph-wiring core; expected, not necessarily harmful.
- **1 package-level giant SCC**, 16 of ~23 `langgraph_engine` subpackages (quality, context_sync,
  preflight_guard, github, build_dependency_resolver, standards, routing, runtime_verification,
  engine_logging, analysis, diagrams, sdlc_pipeline, skills, core, state, + root). Matches the
  archaeology skill's documented anti-pattern of one giant SCC covering 20-40% of modules in legacy
  systems; here it's ~70% of subpackages — the primary decomposition target for any future
  microservice/plugin extraction.
- **Caveat**: AST-only static resolution; `hooks/*` shim files show 0 Ca/Ce since they likely use
  non-dotted/relative import patterns this pass couldn't always match — probably undercounts hooks coupling.

## Path violations (see `path_violations.md` for full detail)
- Home-dir Claude refs (`~/.claude/...`): **116** (13 live code defaults, 103 comments/docstrings/CLI help)
- Absolute path literals (`C:\...`, `/Users/...`): **0**
- Unencoded `open()` calls: **19** — **corrects the orchestrator's baseline of 12**

## Doc drift (see `claude_md_drift.md`): **6 drifts** (3 pre-confirmed + 3 new: docs/releases count
6→7, test-file count 45→63, and undeclared runtime deps for 4 documented integrations)

## Dependencies
- 4 real "used-but-undeclared" third-party packages beyond TTS-adjacent ones: `boto3`/`botocore`,
  `opentelemetry`, `prometheus_client`, `sentry_sdk` — all back documented CLAUDE.md features
  (Secrets Manager AWS SM, Tracing, Metrics Exporter, Error Tracking) yet absent from all three
  requirements files. Also `edge_tts`, `pyttsx3`, `mss`, `pyautogui` used by voice/computer-use
  scripts, undeclared anywhere including `requirements-optional.txt`.
- Several declared packages (aiofiles, bandit, black, coverage, isort, mypy, pre-commit, pylint,
  ruff, safety, watchdog, python-dotenv, pydantic, langchain-core) show no direct top-level import in
  live scope — most are CLI-invoked dev tools (expected); `pydantic`/`watchdog`/`python-dotenv`/
  `langchain-core` merit a closer look (likely transitive-only or imported under a different name).

## Top uncertainty notes
1. Class/function AST counts differ ~1-5% from an independent grep cross-check (173 vs 174 classes,
   616 vs 619 top-level funcs); no parse errors on any of the 335 files, root cause not isolated
   within budget — treat counts as accurate to within a handful.
2. Martin metrics use package granularity = top-level dir + 1 subdir; finer/coarser grouping would
   shift Ca/Ce/I materially, especially for `langgraph_engine` root.
3. All 8 SDP violations trace to one dependent (`langgraph_engine` root/`orchestrator.py`, the
   composition root) depending on higher-instability subpackages — expected for a composition root
   per the skill's own anti-pattern guidance against context-free flagging; not reported as debt.
4. `hooks/` package Ca/Ce likely undercounted (see caveat above) — shims may use import patterns
   (sys.path manipulation, non-dotted imports) this static pass doesn't fully resolve.
5. Coverage was NOT reduced from the requested scope; all four deliverables cover full live scope.
